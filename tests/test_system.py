\"\"\"\nUnit tests for NPP system.\n\"\"\"\n\nimport pytest\nimport torch\nfrom pathlib import Path\nimport tempfile\nimport json\n\n\nclass TestUtilities:\n    \"\"\"Test utility functions.\"\"\"\n    \n    def test_seed_setup(self):\n        \"\"\"Test reproducibility setup.\"\"\"\n        from utils.seed import set_seed\n        \n        set_seed(42)\n        val1 = torch.rand(5)\n        \n        set_seed(42)\n        val2 = torch.rand(5)\n        \n        assert torch.allclose(val1, val2)\n    \n    def test_logger_creation(self):\n        \"\"\"Test logger setup.\"\"\"\n        from utils.logger import get_logger\n        \n        logger = get_logger(\"test_logger\")\n        assert logger is not None\n        assert logger.name == \"test_logger\"\n    \n    def test_config_loading(self):\n        \"\"\"Test configuration loading.\"\"\"\n        from utils.helpers import load_config, save_config\n        \n        with tempfile.TemporaryDirectory() as tmpdir:\n            config = {\"test\": \"value\", \"nested\": {\"key\": \"value\"}}\n            config_path = Path(tmpdir) / \"config.yaml\"\n            \n            save_config(config, config_path)\n            loaded = load_config(config_path)\n            \n            assert loaded[\"test\"] == \"value\"\n            assert loaded[\"nested\"][\"key\"] == \"value\"\n    \n    def test_parameter_counting(self):\n        \"\"\"Test parameter counting utility.\"\"\"\n        from utils.helpers import count_parameters, format_parameters\n        from models.t5_wrapper import T5Wrapper\n        \n        # Create a simple model\n        model = torch.nn.Linear(10, 10)\n        params = count_parameters(model)\n        \n        assert params > 0\n        formatted = format_parameters(params)\n        assert isinstance(formatted, str)\n\n\nclass TestPhraseExtraction:\n    \"\"\"Test phrase extraction module.\"\"\"\n    \n    def test_text_normalization(self):\n        \"\"\"Test text normalization.\"\"\"\n        from preprocessing.phrase_extractor import PhraseExtractor\n        \n        text = \"The Quick BROWN fox!!!  Jumps\"\n        normalized = PhraseExtractor.normalize_text(text, lowercase=True)\n        \n        assert \"quick\" in normalized.lower()\n        assert \"!!!\" not in normalized\n        assert \"  \" not in normalized\n    \n    def test_phrase_filtering(self):\n        \"\"\"Test phrase quality filtering.\"\"\"\n        from preprocessing.phrase_extractor import PhraseExtractor\n        \n        extractor = PhraseExtractor()\n        \n        # Good phrase\n        assert extractor.filter_phrase(\"the quick brown\") is True\n        \n        # Too short\n        assert extractor.filter_phrase(\"a\") is False\n        \n        # Too long\n        assert extractor.filter_phrase(\" \".join([\"word\"] * 25)) is False\n    \n    def test_phrase_position_finding(self):\n        \"\"\"Test phrase position detection.\"\"\"\n        from preprocessing.phrase_extractor import PhraseExtractor\n        \n        sentence = \"The quick brown fox jumps\"\n        phrase = \"quick brown\"\n        \n        pos = PhraseExtractor.get_phrase_position(sentence, phrase)\n        assert pos is not None\n        start, end = pos\n        assert sentence[start:end].lower() == phrase.lower()\n\n\nclass TestMetrics:\n    \"\"\"Test evaluation metrics.\"\"\"\n    \n    def test_exact_match(self):\n        \"\"\"Test exact match metric.\"\"\"\n        from evaluation.metrics import MetricsCalculator\n        \n        score = MetricsCalculator.exact_match(\"test\", \"test\")\n        assert score == 1.0\n        \n        score = MetricsCalculator.exact_match(\"test\", \"other\")\n        assert score == 0.0\n    \n    def test_bleu_score(self):\n        \"\"\"Test BLEU score calculation.\"\"\"\n        from evaluation.metrics import MetricsCalculator\n        \n        ref = \"the cat sat on the mat\"\n        hyp = \"the cat sat on a mat\"\n        \n        scores = MetricsCalculator.bleu_score(ref, hyp)\n        assert 0 <= scores[\"bleu\"] <= 1\n        assert \"bleu_1\" in scores\n    \n    def test_rouge_score(self):\n        \"\"\"Test ROUGE score calculation.\"\"\"\n        from evaluation.metrics import MetricsCalculator\n        \n        ref = \"the cat sat on the mat\"\n        hyp = \"the cat sat on the mat\"\n        \n        scores = MetricsCalculator.rouge_score(ref, hyp)\n        assert \"rouge1\" in scores\n        assert \"rouge2\" in scores\n        assert \"rougeL\" in scores\n    \n    def test_partial_match(self):\n        \"\"\"Test partial match metric.\"\"\"\n        from evaluation.metrics import MetricsCalculator\n        \n        # Identical\n        score = MetricsCalculator.partial_match(\"test\", \"test\")\n        assert score == 1.0\n        \n        # No overlap\n        score = MetricsCalculator.partial_match(\"test\", \"other\")\n        assert score == 0.0\n        \n        # Partial overlap\n        score = MetricsCalculator.partial_match(\"test word\", \"test\")\n        assert 0 < score < 1\n\n\nclass TestDatasetBuilder:\n    \"\"\"Test NPP dataset building.\"\"\"\n    \n    def test_npp_example_creation(self):\n        \"\"\"Test NPP example creation.\"\"\"\n        from preprocessing.dataset_builder import NPPDatasetBuilder, NPPExample\n        \n        builder = NPPDatasetBuilder()\n        \n        example = builder.create_npp_example(\n            sentence=\"The quick brown fox\",\n            phrase_groups={\"NP\": [\"quick brown fox\"], \"VP\": []}\n        )\n        \n        if example:\n            assert isinstance(example, NPPExample)\n            assert \"generate next phrase\" in example.input_text\n            assert len(example.target_phrase) > 0\n    \n    def test_example_to_dict(self):\n        \"\"\"Test example conversion to dictionary.\"\"\"\n        from preprocessing.dataset_builder import NPPExample\n        \n        example = NPPExample(\n            input_text=\"test input\",\n            target_phrase=\"test phrase\",\n            phrase_type=\"NP\",\n            full_sentence=\"full test sentence\"\n        )\n        \n        d = example.to_dict()\n        assert isinstance(d, dict)\n        assert d[\"input_text\"] == \"test input\"\n        assert d[\"target_phrase\"] == \"test phrase\"\n\n\nclass TestTokenizer:\n    \"\"\"Test tokenizer module.\"\"\"\n    \n    def test_tokenizer_initialization(self):\n        \"\"\"Test tokenizer creation.\"\"\"\n        from preprocessing.tokenizer import NPPTokenizer\n        \n        tokenizer = NPPTokenizer(\"t5-base\")\n        assert tokenizer is not None\n        assert tokenizer.vocab_size > 0\n    \n    def test_tokenization(self):\n        \"\"\"Test text tokenization.\"\"\"\n        from preprocessing.tokenizer import NPPTokenizer\n        \n        tokenizer = NPPTokenizer(\"t5-base\")\n        result = tokenizer.tokenize(\"test text\", return_tensors=None)\n        \n        assert \"input_ids\" in result or result is not None\n\n\nif __name__ == \"__main__\":\n    pytest.main([__file__, \"-v\"])